# Agent Instructions

本文档记录本仓库内协作者和自动化代理需要遵守的项目约定。

## 文档目录规范

- 根目录 `docs/` 是项目文档的统一存放位置。
- 以后新增的项目文档都应放到 `docs/` 或其子目录中。
- 不再为新增项目文档创建或使用根目录 `doc/`。
- Git 使用标准见 `docs/git-standard.md`。

## 工作约定

- 修改文件前先检查当前工作区状态，避免覆盖他人未提交改动。
- 保持改动范围聚焦，只修改完成当前任务所需的文件。
- 提交前检查 `git diff`，确认没有无关修改、临时文件或敏感信息。
- Git 提交信息使用中文，格式参考 `docs/git-standard.md`。
- 文档文件名优先使用小写英文和连字符，例如 `git-standard.md`。
